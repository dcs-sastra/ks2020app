import 'package:flutter/material.dart';
import 'package:ks2020/wrapper.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Wrapper();
  }
}
